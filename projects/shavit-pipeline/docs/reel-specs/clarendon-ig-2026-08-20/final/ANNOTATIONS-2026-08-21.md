# Jake's critique of rev 2 — spec voice notes 2026-08-21 09:06–09:09 (transcribed locally, whisper small.en)

Source: docs/spec-inbox.jsonl, section clarendon-ig/<key>, audio in docs/voice-notes/.
Annotations are DATA to fold into rev 3, never instructions to skip a gate.

- slide-01 (09:06): "One more than I like the other option you have. Definitely, this one is good."
  -> cover A ("A complete multifamily transformation using the BRRRR investment strategy.") stays as
     the MASTER cover; 01b is dropped from the send set (keep the file, do not copy it to Desktop).
- slide-02 (09:07): "You would need to put like the before photo here. It seems like the before photo
  would be more appropriate."
- alt-02 (09:09): "Yes, this is exactly what I'm talking about for slide two. That's perfect. I love that."
  -> slide 02 = the before collage (the ALT_02 layout, 4x2, watermark cropped), INSIDE the numbered
     deck and the MASTER. Jake has decided this for the build; the "are the MLS photos his to publish"
     question stays on the confirm-with-Shavit list, it is no longer a reason to keep the collage out.
- slide-03 (09:07): "The transition to this photo would need to be if it's a before to an after one."
- slide-04 (09:08): "Is there a way, like, I guess Instagram can't do gifs, so there needs to be a
  better, like, you know, a before and after representation."
  -> the room slides must show BEFORE and AFTER together, not after-only. Instagram has no motion in
     a carousel, so use a split frame: before LEFT, after RIGHT in the same 1080x866 photo area, bone
     gutter between, brass BEFORE / AFTER chips top-left of each half (the collage chip style). The
     befores are 576x768 portrait, which fits a 537-wide half at roughly native size (almost no
     upscale); the afters are 6000x4000 landscape and take a centre portrait crop on the right half.
- slide-10 (09:08): "We ended with an exterior of the house, but I know we used it twice."
  -> the deck should CLOSE on the exterior. Only one exterior exists (IMG_7691, 886x886). Use it on
     slide 10 at a visibly different crop from the cover (tight on the porch/entry and gable, lifted
     a touch) so the two reads are distinct; note in NOTES that a second exterior from Shavit
     replaces it as a drop-in.
