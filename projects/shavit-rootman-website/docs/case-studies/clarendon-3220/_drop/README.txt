Drop new photos here.

Any format sips reads (jpg, png, heic). Original resolution is fine, do not
pre-crop: wire-pair.sh centre-crops both frames to an identical 1400x933 so the
slider wipe lines up.

If you can, name them so the pairing is obvious, e.g.
  bedroom-before.jpg / bedroom-after.jpg
Otherwise just drop them and say which is which.
